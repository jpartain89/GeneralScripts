---
# auto_bootstrap.yml

- hosts: all

  vars:
    - { git_os: linux, when:  }

  tasks:
    - include: ./tasks/git_pull_linux.yml
      when: ansible_os_family == "Debian"
    - include: ./tasks/git_pull_macos.yml
      when: ansible_os_family == "Darwin"
