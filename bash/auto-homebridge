#!/bin/bash -e

source allunix

trap 'killall homebridge; exit $?' SIGHUP SIGINT SIGTERM

cd /usr/local/lib/node_modules/homebridge-wink &&
/usr/local/bin/homebridge -I -U /Users/jpartain89/.homebridge >> logfile &
echo $! > /var/run/homebridge.pid
